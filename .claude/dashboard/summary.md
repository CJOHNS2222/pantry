# Claude Summary

## Priorities

- **Run dep-auditor agent** — Dependency audit routine still overdue (never completed, 30-day cadence); AUDIT_DEPS.md status is fail with 10 findings.
- **Commit the API 36 upgrade** — compileSdk/targetSdk 36 + `enableOnBackInvokedCallback` manifest flag are edited but uncommitted; build and smoke-test back gesture on an API 36 emulator, then commit.
- **Start FIXES.md Batch 1 (F4–F16)** — F4 (LeftoverAnalytics) and F13 (XSS) first: single-file, S-effort, real user/security impact. Run code-fixer agent.
- **Product call on Batch 4 re-wires (F48–F50)** — UseSoonRecommendations, camera-permission dedupe, RecipeCommunityInsights need keep/re-wire decision; route through architect-reviewer agent.
- **Fold new audit results into FIXES.md** — fresh code-audit.md and bug-audit.md are being regenerated by in-flight subagents; run fix-planner agent when they land.

## Notes

- Full-audit, release (v3.0.15), and graphify-update routines all completed 2026-07-31 — on cadence.
- AUDIT_API.md, AUDIT_DB.md, AUDIT_INFRA.md still status fail from the 2026-07-30 pass; their items are already batched in FIXES.md.
- Fixes counter reads 0/0 — FIXES.md uses F-numbered headings, not checkboxes, so the parser cannot track completion.
