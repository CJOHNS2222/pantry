# Firebase Cloud Functions Environment Variables
# These variables should be set using Firebase CLI:
# firebase functions:config:set gmail.user="your-email@gmail.com" gmail.password="your-app-password"
# firebase functions:config:set app.url="https://smartpantry.app"

# Or set them in .env.local and use: firebase functions:config:set --local

# Gmail configuration
# To use Gmail with nodemailer:
# 1. Enable 2FA on your Gmail account
# 2. Generate an "App Password" (not your regular password)
# 3. Use the app password below

EMAIL_USER=your-gmail@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# App configuration
APP_URL=https://smartpantry.app